package hanoi;

class Stack {

    private static int length;

    private Disk top;

    Stack(int length) {
        Stack.length = length;
        top = new Disk();
    }

    // Moves the topmost disk in this Stack to s
    void moveTop(Stack s) {

    }
}
